services:
  - type: web
    name: classroom-experiments
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT 'app:create_app()'"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: HOST_TOKEN
        value: classroom2026
      - key: DATABASE_PATH
        value: /data/app.db
      - key: PYTHON_VERSION
        value: "3.12"
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
